CREATE TABLE TabB(id INTEGER NOT NULL primary key autoincrement,
Added DATETIME NOT NULL,
Updated DATETIME NOT NULL,
Userid	INTEGER,
DatetimeField	DATETIME,
Skip	VARCHAR(1),
N1k	INTEGER,
MidText	VARCHAR(21),
MoneyVal	REAL,
Fkey	INTEGER,
CONSTRAINT FK_TabB_Fkey FOREIGN KEY (Fkey)    REFERENCES TabA(NumberField),
CONSTRAINT FK_TabB_Userid FOREIGN KEY (Userid)    REFERENCES Account(Id));
